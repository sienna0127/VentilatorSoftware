dist: bionic

addons:
  apt:
    update: true

jobs:
  include:
    - stage: "Test"
      before_install:
        - sudo apt-get install docker.io
        - sudo docker --version
      install:
        - true
      before_script:
        - true
      script:
        - true
